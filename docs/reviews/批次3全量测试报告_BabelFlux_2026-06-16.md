# BabelFlux 批次 1-3 全量测试报告

日期：2026-06-16
分支：`feat/tts-bilingual-playback`
基线：`1219cae feat: add bilingual tts playback`

## 测试环境

- Python: 3.11.8
- Node.js: v20.20.2
- npm: 10.8.2
- 后端测试模式：mock provider，`backend/tests/conftest.py` 强制不触达真实云端 API

## 批次 3 代码审查结论

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| `pipeline.py` 增加 audio 分支 | 通过 | `_handle` 分发 `kind == "audio"` 到 `_on_audio`；外层 `_consume` 统一捕获异常并下发 error |
| response_id 到 segment_id 关联 | 通过 | `_on_audio` 对未绑定 response 的音频写入 `_pending_audio_by_response`，`_on_translation` 绑定后 `_flush_pending_audio` |
| `AudioSegment` 模型字段 | 通过 | `segmentId` / `audioBase64` / `sampleRate` 均由 Pydantic alias 输出 |
| TTS 输出格式与采样率 | 通过 | DashScope session.update 明确 `output_audio_format="pcm"`；pipeline 使用 `TTS_OUTPUT_SAMPLE_RATE=24000` 下发 sampleRate |
| `useTtsPlayback` 采样率 | 通过 | `AudioContext({ sampleRate })` 与 `createBuffer(..., sampleRate)` 动态使用服务端 sampleRate |
| 播放队列拼接 | 通过 | `nextStartTime = startAt + buffer.duration`，避免片段抢占当前播放时间 |
| 静音/音量 | 通过 | GainNode 在 `setMuted` / `setVolume` 中更新，测试覆盖增益变化 |
| 前端事件门控 | 通过 | `audio_segment` 仅在当前表单 `ttsEnabled` 开启时进入播放队列 |
| UI 控件 | 通过 | QuickSetupPanel/SettingsDialog 有语音播报开关；MediaPanel 有静音按钮和音量滑块 |
| 测试覆盖 | 通过 | 后端 audio/pending audio、前端播放队列、App 集成 payload 与播放均有测试 |

## 测试结果汇总

| 测试项 | 状态 | 结果 | 备注 |
| --- | --- | --- | --- |
| 后端单元/集成全量 | 通过 | 68/68 passed | 无 warnings summary |
| 后端代码质量 | 通过 | All checks passed | `ruff check app tests` |
| 后端 E2E fixture | 通过 | 2/2 passed | mock 完整事件流，默认无 `audio_segment` |
| 前端类型检查 | 通过 | 0 errors | `node_modules/.bin/vue-tsc --noEmit` |
| 前端单元测试 | 通过 | 5 files / 36 tests passed | verbose reporter |
| 前端构建 | 通过 | success | `frontend/dist/index.html` 和 assets 已生成 |
| 桌面端类型检查 | 通过 | 0 errors | `desktop/node_modules/.bin/vue-tsc --noEmit` |
| 桌面端构建 | 通过 | success | `desktop/dist/index.html` 和 assets 已生成；Vite 有 chunking 提示 |

## 执行命令

```bash
cd backend && PYTHONPATH=. python3 -m pytest -v --tb=short
cd backend && ruff check app tests
cd frontend && node_modules/.bin/vue-tsc --noEmit
cd frontend && node_modules/.bin/vitest run --reporter=verbose
cd frontend && npm run build
cd desktop && node_modules/.bin/vue-tsc --noEmit
cd desktop && npm run build
```

## 新增测试与测试基础设施

- 新增 `backend/tests/test_e2e_fixture.py`：覆盖 mock 模式完整事件流，并确认 TTS 默认关闭时不产生 `audio_segment`。
- 新增 `backend/tests/asgi_test_client.py`：使用 `httpx.ASGITransport` 和轻量 WebSocket ASGI session 取代 Starlette/FastAPI `TestClient`，避免当前 Starlette 版本在测试导入阶段产生第三方弃用告警。
- 迁移 `test_health.py`、`test_model_gateway_api.py`、`test_sessions.py` 到本地 ASGI 测试客户端；REST 网关鉴权与 WS 鉴权仍由测试覆盖。
- 补充 `test_live_translate_session_update_declares_low_latency_pcm_rate` 对 `output_audio_format="pcm"` 的断言。

## 功能验证推导

- TTS 端到端接通：后端 `audio.delta` 处理、pending audio 绑定、前端 `audio_segment` 门控播放、音量/静音均由测试覆盖。
- 字幕分句质量改善：批次二的 CJK 重复折叠、长句拆分、partial overlap 合并和 source-only 过滤测试均在本轮后端/前端全量回归中通过。
- 鉴权生效：主 WebSocket 缺失/错误 token 拒绝、handoff WS token 校验、REST model gateway 开关鉴权均有测试覆盖。
- mock 本地链路：E2E fixture 与 WebSocket mock 流均确认 source sync、字幕、译文、纠偏、报告闭环可用。

## 遗留问题清单

| 问题 | 阻塞 Demo | 优先级 | 建议 |
| --- | --- | --- | --- |
| TTS 实际听感与浏览器 autoplay 策略 | 否 | P2 | Demo 前用真实浏览器听测一次，确认用户手势解锁与音质 |
| 真实 DashScope 音频采样率长期兼容 | 否 | P2 | 当前真实样例与 pipeline 常量均为 24kHz；若厂商协议变更需接入动态采样率 |
| 字幕按真实音频时长强制切分 | 否 | P3 | 批次二已修重复与长句展示；真实链路时长阈值可下个迭代继续收紧 |
| 桌面端 Vite chunking 提示 | 否 | P3 | `@tauri-apps/api/window.js` 同时静态/动态导入，仅影响 chunk 拆分，可后续整理 |

## 结论

批次三 TTS 改动经过 diff review、测试覆盖审查、本地 E2E fixture、后端/前端/桌面全量回归与构建验证。当前证据支持批次 1-3 在本地测试范围内无破坏性回归，可以继续推进 PR 合并前的真实浏览器听测与前置 PR retarget 流程。
